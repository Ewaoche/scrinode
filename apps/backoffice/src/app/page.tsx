import { Dashboard } from '../components/dashboard';

/**
 * Backoffice landing.
 *
 * Scaffold only. Admin identity, RBAC and every real feature arrive in
 * Stage 2 onward — see docs/PLAN_backoffice_architecture.md.
 */
export default function Home() {
  return <Dashboard />;
}
